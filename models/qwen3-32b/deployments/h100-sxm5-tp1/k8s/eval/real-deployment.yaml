apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm-real
  labels:
    app: vllm-real
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vllm-real
  template:
    metadata:
      labels:
        app: vllm-real
    spec:
      # Pin to H100 SXM5 nodes — the physics latency config is hardcoded to H100
      # SXM5 constants (peak_tflops 989, hbm_gbps 3350). Replace the label below
      # with your cluster's actual H100 node label before applying; scheduling on
      # a different GPU silently invalidates the comparison.
      nodeSelector:
        nvidia.com/gpu.product: NVIDIA-H100-80GB-HBM3  # <-- verify/replace for your cluster
      # If your GPU nodes are tainted, uncomment and adjust:
      # tolerations:
      # - key: nvidia.com/gpu
      #   operator: Exists
      #   effect: NoSchedule
      containers:
      - name: vllm
        image: vllm/vllm-openai:latest
        command:
        - vllm
        - serve
        - Qwen/Qwen3-32B
        args:
        - --served-model-name
        - qwen3-32b
        - --tokenizer
        - Qwen/Qwen3-32B
        - --tensor-parallel-size
        - "1"
        - --gpu-memory-utilization
        - "0.9"
        - --max-model-len
        - "8192"
        env:
        - name: HF_HOME
          value: /tmp/hf
        - name: HOME
          value: /tmp
        - name: XDG_CACHE_HOME
          value: /tmp/cache
        - name: HF_TOKEN
          valueFrom:
            secretKeyRef:
              name: hf-token
              key: token
              optional: true
        ports:
        - containerPort: 8000
          name: http
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 600
          periodSeconds: 10
          failureThreshold: 6
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 600
          periodSeconds: 10
          failureThreshold: 3
        resources:
          requests:
            memory: 32Gi
            nvidia.com/gpu: 1
          limits:
            memory: 32Gi
            nvidia.com/gpu: 1
        volumeMounts:
        - name: hf-cache
          mountPath: /tmp/hf
      volumes:
      - name: hf-cache
        emptyDir:
          sizeLimit: 80Gi
